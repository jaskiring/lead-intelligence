# config.py

ADMIN_PASSWORD = st.secrets["auth"]["admin_password"]
REP_PASSWORDS = st.secrets["auth"]["reps"]

SHEET_NAME = "lead_intelligence_db"
WORKSHEET_NAME = "leads_master"

LEAD_KEY_FIELD = "phone"   # later can change to CRM ID